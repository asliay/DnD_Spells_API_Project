


const RulesComponent = ({handleClick, races, classes, spells}) => {
  // const raceInfo = races.map((race) => {
  //   return (<Character key={race.index} race = {race}/>)
  // });

  // const classInfo = classes.map((charClass) => {
  //   return charClass
  // });

  return (
    <>
      {/* {raceInfo} */}
      
      <button onClick={handleClick}>Click me</button>
    </>
  )
}

export default RulesComponent;